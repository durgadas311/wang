#include "wang600.h"

#include "libplot/plotter.w6h"

BEGIN()
	CLEAR()
	ENTER(0)
	ST(startX)
	ENTER(360)
	ST(maxX)
	ENTER(5)
	ST(stepX)
	ENTER(1)	// scaling in X
	ST(scalX)
	ENTER(200)	// scaling in Y
	ST(scalY)
	ENTER(10)	// translation in X
	ST(tranX)
	ENTER(210)	// translation in Y
	ST(tranY)

	FCALL(plotter)
	END_PROG()

MARK(DESCRIPTION_FUNCTION)
	ALPHA_PLOT("Y = SIN{X}")
	RETURN()

MARK(GRAPHING_FUNCTION)
	SIN()
	RETURN()
END()
