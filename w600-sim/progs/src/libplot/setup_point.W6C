#include "wang600.h"

#include "plotter.w6h"

// Labels:
LABEL(setup_point)

BEGIN()
MARK(setup_point)
	// compute relative change from last point
	RE(convX)
	ST(plotX)
	EXCHG(lastX)
	SUB(plotX)
	RE(convY)
	ST(plotY)
	EXCHG(lastY)
	SUB(plotY)
	RETURN()
END()
